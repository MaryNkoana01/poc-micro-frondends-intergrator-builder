import { DefaultTheme } from 'styled-components';
export declare const IconBtnMixin: ({ floats, colors, typography }: DefaultTheme) => import("styled-components").RuleSet<object>;
export declare const iconMixin: ($showLabel: boolean, primaryColor: boolean, { colors }: DefaultTheme) => import("styled-components").RuleSet<object>;
