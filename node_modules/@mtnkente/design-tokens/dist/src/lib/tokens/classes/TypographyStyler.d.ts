import { FileStylesResponse, NodesResponse, ThemeTypography } from '../models';
export declare class TypographyStyler {
    processStyles(fileStyles: FileStylesResponse): string[];
    private checkNodeName;
    processNodes(nodesResponse: NodesResponse): ThemeTypography;
    private processTextTransform;
    /**
     * The WorkSans font has an incorrect name specified in the file itself. This fixes this problem
     * @param fontName PostScript font name to adjust
     */
    private correctFontName;
}
